//initial pull request
test push

//Below is fake java to test program on
//This should succeed
String[] text = {"a", "b", "c"}
public void method(int a, int b, int c, int[] d) {
    int y = (((a * b) / c) + d[1]) + a;

}